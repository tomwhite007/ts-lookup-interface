export interface CustomerFeatureSalesData {
  product: string | null;
  option: string | null;
  mobileNumber: string | null;
}

export interface CustomerFeaturePurchaseData {
  mobileNumber: string | null;
  emailAddress: string | null;
}
